[
  {
    "id": "version-post-knowledge-cutoff-github-actions",
    "reason": "LLMs flag GitHub Action versions released after their training cutoff as 'nonexistent' or claim an older major version is 'latest'. The verify field causes the skill to confirm the tag exists via the GitHub API before accepting the suppression — if the version genuinely does not exist (404), the finding is kept.",
    "verify": "github-release",
    "match": {
      "pattern": "(does not exist as a released version|is not a (released|valid|published) version|no such (release|version|tag) exists|(latest|current) (stable )?release is v[0-9]+|unreleased version|references? a non-existent (tag|version|release)|may not be a (published|valid|released) (release|version|tag)|might not (exist|be published|be a valid)|not (yet )?published|could not (be verified|exist)).{0,300}actions/|actions/.{0,300}(does not exist as a released version|is not a (released|valid|published) version|no such (release|version|tag) exists|(latest|current) (stable )?release is v[0-9]+|unreleased version|references? a non-existent (tag|version|release)|may not be a (published|valid|released) (release|version|tag)|might not (exist|be published|be a valid)|not (yet )?published|could not (be verified|exist))"
    }
  },
  {
    "id": "version-post-knowledge-cutoff-npm",
    "reason": "LLMs flag npm package versions released after their training cutoff as 'nonexistent'. The verify field causes the skill to confirm existence on registry.npmjs.org before suppressing.",
    "verify": "npm",
    "match": {
      "pattern": "(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version).{0,200}[a-zA-Z0-9._-]+@[0-9]|[a-zA-Z0-9._-]+@[0-9].{0,200}(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version)"
    }
  },
  {
    "id": "version-post-knowledge-cutoff-pypi",
    "reason": "LLMs flag PyPI package versions released after their training cutoff as 'nonexistent'. The verify field causes the skill to confirm existence on pypi.org before suppressing.",
    "verify": "pypi",
    "match": {
      "pattern": "(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version).{0,200}[a-zA-Z0-9._-]==[0-9]|[a-zA-Z0-9._-]==[0-9].{0,200}(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version)"
    }
  },
  {
    "id": "version-post-knowledge-cutoff-go-module",
    "reason": "LLMs flag Go module versions released after their training cutoff as 'nonexistent'. The verify field causes the skill to confirm existence on proxy.golang.org before suppressing.",
    "verify": "go-module",
    "match": {
      "pattern": "(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version).{0,200}[a-zA-Z0-9._-]+\\.[a-zA-Z]{2,}/[a-zA-Z0-9._/@-]+@v[0-9]|[a-zA-Z0-9._-]+\\.[a-zA-Z]{2,}/[a-zA-Z0-9._/@-]+@v[0-9].{0,200}(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version)"
    }
  },
  {
    "id": "version-post-knowledge-cutoff-cargo",
    "reason": "LLMs flag Rust crate versions released after their training cutoff as 'nonexistent'. The verify field causes the skill to confirm existence on crates.io before suppressing.",
    "verify": "cargo",
    "match": {
      "pattern": "(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version).{0,200}[a-zA-Z0-9_-]+ = \"[0-9]|[a-zA-Z0-9_-]+ = \"[0-9].{0,200}(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version)"
    }
  },
  {
    "id": "version-post-knowledge-cutoff-docker-hub",
    "reason": "LLMs flag Docker Hub image tags released after their training cutoff as 'nonexistent'. The verify field causes the skill to confirm existence on hub.docker.com before suppressing. Private registries (GHCR, GCR, ECR) are excluded as they require auth.",
    "verify": "docker-hub",
    "match": {
      "pattern": "(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such (tag|version|image)).{0,200}([a-zA-Z0-9._-]+/)?[a-zA-Z0-9._-]+:[0-9]|([a-zA-Z0-9._-]+/)?[a-zA-Z0-9._-]+:[0-9].{0,200}(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such (tag|version|image))"
    }
  },
  {
    "id": "version-post-knowledge-cutoff-ruby",
    "reason": "LLMs flag Ruby gem versions released after their training cutoff as 'nonexistent'. The verify field causes the skill to confirm existence on cache.ruby-lang.org before suppressing.",
    "verify": "ruby-org",
    "match": {
      "pattern": "(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version).{0,200}gem [\"'][a-zA-Z0-9_-]+[\"'], [\"'][0-9]|gem [\"'][a-zA-Z0-9_-]+[\"'], [\"'][0-9].{0,200}(does not exist|unreleased|not (a |an )?(valid |published |released )?version|no such version)"
    }
  },
  {
    "id": "upload-download-artifact-version-mismatch-false-positive",
    "reason": "actions/upload-artifact and actions/download-artifact have intentionally offset major version numbers — they are separate packages released independently. Flagging mismatched major versions as incompatibility is a false positive for this specific action pair.",
    "match": {
      "pattern": "upload-artifact.{0,200}download-artifact|download-artifact.{0,200}upload-artifact"
    }
  },
  {
    "id": "trufflehog-unverified-test-file",
    "reason": "Unverified secret detections in test/fixture/example files are almost always intentional mock data. The run-trufflehog.sh script already demotes these to Low/confidence-40 and tags them '[test file — likely mock data]', but this rule provides a safety net.",
    "match": {
      "pattern": "\\(unverified\\).{0,50}\\[test file"
    }
  }
]
